<manifest xmlns:android="http://schemas.android.com/apk/res/android">

	<application>
		<provider
			android:name=".DebuggerInstaller"
			android:authorities="${applicationId}.debugger-installer"
			android:enabled="true"
			android:exported="false" />

		<activity
			android:name=".screen.wireframe.WireframeActivity"
			android:theme="@style/SldFullscreen" />

		<activity
			android:name=".screen.screenshot.ScreenshotActivity"
			android:theme="@style/SldFullscreen" />

		<activity
			android:name=".screen.SettingsActivity"
			android:theme="@style/SldDialog" />
	</application>
</manifest>